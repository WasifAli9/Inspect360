# ============================================================
# Inspect360 - Production Environment Variables Template
# ============================================================
# IMPORTANT: Copy this file to .env.production and fill in values
# DO NOT commit the filled .env.production file to version control
# ============================================================

# ===================
# DATABASE CONFIGURATION
# ===================
# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database?sslmode=require
DATABASE_URL=postgresql://inspect360_user:YOUR_SECURE_PASSWORD@your-postgres-host:5432/inspect360?sslmode=require

# Individual PostgreSQL settings (used by some database tools)
PGHOST=your-postgres-host
PGPORT=5432
PGUSER=inspect360_user
PGPASSWORD=YOUR_SECURE_PASSWORD
PGDATABASE=inspect360

# ===================
# APPLICATION SETTINGS
# ===================
# Environment mode
NODE_ENV=production

# Server port (IIS will override this)
PORT=5000

# Base URL for the application (your production domain)
BASE_URL=https://yourdomain.com

# Session secret (generate a random 64-character string)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=YOUR_64_CHARACTER_RANDOM_STRING_HERE

# ===================
# STRIPE PAYMENT INTEGRATION
# ===================
# Get these from https://dashboard.stripe.com/apikeys
# Use LIVE keys for production (not test keys)
STRIPE_SECRET_KEY=sk_live_YOUR_STRIPE_SECRET_KEY
STRIPE_PUBLISHABLE_KEY=pk_live_YOUR_STRIPE_PUBLISHABLE_KEY

# Stripe Webhook Secret (from Stripe Dashboard > Webhooks)
# Create a webhook endpoint pointing to: https://yourdomain.com/api/webhooks/stripe
STRIPE_WEBHOOK_SECRET=whsec_YOUR_WEBHOOK_SECRET

# ===================
# EMAIL SERVICE (RESEND)
# ===================
# Get API key from https://resend.com/api-keys
RESEND_API_KEY=re_YOUR_RESEND_API_KEY

# Email sender address (must be verified in Resend)
RESEND_FROM_EMAIL=noreply@yourdomain.com

# ===================
# OPENAI / AI INTEGRATION
# ===================
# Get API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-YOUR_OPENAI_API_KEY

# Optional: Custom base URL for OpenAI-compatible APIs
# OPENAI_BASE_URL=https://api.openai.com/v1

# ===================
# GOOGLE CLOUD STORAGE (for file uploads)
# ===================
# Service account credentials (JSON string or path to JSON file)
# Create at https://console.cloud.google.com/iam-admin/serviceaccounts
GCS_BUCKET_NAME=your-gcs-bucket-name
GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account.json

# ===================
# SECURITY SETTINGS
# ===================
# CORS allowed origins (comma-separated for multiple domains)
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Rate limiting (requests per minute per IP)
RATE_LIMIT_REQUESTS_PER_MINUTE=100

# ===================
# OPTIONAL: TENANT PORTAL URL
# ===================
# If tenant portal is on a separate domain
TENANT_PORTAL_URL=https://portal.yourdomain.com

# ============================================================
# END OF CONFIGURATION
# ============================================================
